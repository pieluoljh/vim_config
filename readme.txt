this is my vim config in git hub,
it is also my first repo in git hub.
yalin github: https://github.com/huaihuaiVV/my_vimrc.git
tools folder is to save some tools specially installed in my MACOS
how to use it?
  1, git clone to the local
  2, "git submodule update --init" this is used to install submodules to introduce all of insert function calls
  3, "source ~/Documents/sourcecode/vim_config/.vimrc" to be written to ~/.vimrc
  4, "source ~/Documents/sourcecode/vim_config/bash/mybash.sh" to be written to ~/.bash_profile 
  5, gen_tags
